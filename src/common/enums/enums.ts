export enum PagePath {
  LOGIN = '/',
  MAIN = '/main',
  GAME = '/game',
  STATISTICS = '/statistics',
  NOT_FOUND = '/not-found',
}

export enum ImgAlts {
  exitIcon = 'Exit',
  logoIcon = 'Logo',
}
